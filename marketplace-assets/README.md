# Marketplace Assets

## Required for VS Code Marketplace listing:

### Screenshots (recommended 1280x720 or 1920x1080):
1. `chat-demo.png` — Chat panel with conversation, code blocks, streaming
2. `inline-edit-demo.png` — Cmd+K inline edit with diff preview
3. `completion-demo.png` — Tab completion with ghost text
4. `agent-demo.png` — Agent mode with plan and execution
5. `search-demo.png` — Codebase semantic search results
6. `themes-demo.png` — Multiple theme presets side by side

### Videos (optional):
1. `quickstart.gif` — 30s getting started demo
2. `agent-flow.gif` — Agent mode full flow

### Notes:
- Screenshots should be taken on a real project (not hello world)
- Use dark theme for consistency
- Show meaningful code (not lorem ipsum)
- Ensure no secrets or personal data visible in screenshots
- Recommended: record using VS Code's built-in screen capture
- GIF files should be < 5MB each

### To add screenshots to README:
Add to README.md under each feature section:
```
![Chat Demo](marketplace-assets/chat-demo.png)
```
